/*
 * Shared class for the secondary ("Sign in" / "Talk to sales") CTA on the dark teal→blue
 * gradient sections (hero + final CTA). Used with <ShimmerButton> so the secondary CTAs
 * are styled identically. (Primary "Get started" CTAs use <MorphButton tone="onDark">.)
 */
export const ctaSecondaryDark =
  'inline-flex h-[50px] items-center justify-center rounded-[10px] px-6 text-sm font-medium border border-white/60 text-white'
